<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.edwin.dao.UserDao">

    <!--save-->
    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into user values (#{id},#{username},#{email},#{password},#{user_balance},#{status},#{updated_at},#{created_at})
    </insert>

    <!--find by user name-->
    <select id="findByUserName" parameterType="String" resultType="User">
        select id,username,email,password,user_balance,status,updated_at,created_at
        from user where username = #{username}
    </select>


    <!--find by user email-->
    <select id="findByUserEmail" parameterType="String" resultType="User">
        select id,username,email,password,user_balance,status,updated_at,created_at
        from user where email = #{email}
    </select>

    <!--find by user id-->
    <select id="findByUserId" parameterType="String" resultType="User">
        select id,username,email,password,user_balance,status,updated_at,created_at
        from user where id = #{id}
    </select>
</mapper>